# ============================================================
# FRONTEND PRODUCTION ENV (committed example — copy values into Vercel)
# ============================================================
# Paste into Vercel:
#   Project → Settings → Environment Variables → Production
#
# Replace the placeholder with the Render backend URL Render gives
# you after the backend service is created. NO trailing slash.

VITE_API_URL=https://deencore-backend.onrender.com
